# Telegram AI Control Plane Status — 2026-08-29

## User requirements locked

1. MiniPC OpenCodex path must expose Antigravity and OpenCode Go DeepSeek Flash.
2. Telegram Codex selection must use direct Codex account/CLI usage, bypassing OpenCodex.
3. Telegram must be able to instruct an AI worker to inspect/change/test/deploy the project behind `hub.avocadoss.co.kr` through the canonical Git/deploy workflow.

The binding implementation contract is `docs/TELEGRAM_AI_CONTROL_PLANE_CONTRACT.md`.
The executable repair task for Codex is `ai/tasks/TELEGRAM_AI_CONTROL_PLANE_REPAIR.md`.

## Repository work completed

- Added a dedicated control-plane contract with explicit route identities, no-silent-fallback rule, UTF-8/idempotency/timeout requirements, safe WholesaleHub control boundaries and real acceptance tests.
- Added a Codex repair task that requires discovery of the real MiniPC Telegram router/service/OpenCodex runtime before any edit.
- Added `scripts/telegram-ai-control-plane-diagnose.sh` and `.ps1`.
- Diagnostic adds OpenCodex port 10100 probing, router source/service discovery, direct Codex/OpenCode/Antigravity binary discovery, OpenCode DeepSeek Flash isolated smoke, sandbox/userns evidence, UTF-8 evidence and canonical project checks.
- Diagnostic intentionally does not auto-run direct Codex or Antigravity until their real runtime invocation/adapter is discovered; it does not change sysctl, restart services or modify Production.
- Added CI contract tests and Windows/Linux syntax checks. First CI run for commit `69e882686331d06e09a66fb88864e76503677ae9` passed both jobs.

## Production/catalog/order evidence from user run

Deployment of prior main `be5f84bf9f2571a1f190f4e6646457edefb70523` succeeded with public homepage/search HTTP 200 and managed plugin/MU-plugin verification.

The read-only operations diagnostic at 2026-08-29 09:52 KST showed:

- DailyFood snapshot: complete, fresh, 132 products / 663 options.
- Walldo snapshot: complete, fresh, 30 products / 157 options.
- Catalog runtime: completed, exit 0, last successful run 2026-08-28 18:01 KST-equivalent source timestamp.
- WordPress container: running; bootstrap exit 0.
- Order exporter screening: 0 pending rows, 0 pending orders, 0 pending-before-07:00 rows, 0 source-unmapped orders.
- Therefore the observed 07:00 zero-order Telegram report is consistent with current exporter state, and the 08:13 Walldo warning was consistent with the old false-positive freshness rule rather than proof of Telegram or order-system failure.

## Remaining runtime blocker

The actual MiniPC Telegram router source/service and OpenCodex runtime have not yet been returned to ChatGPT from a runtime diagnostic. GitHub repository inspection cannot prove or repair that MiniPC-only runtime safely.

Next required evidence is generated by:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\scripts\telegram-ai-control-plane-diagnose.ps1"
```

After that report identifies the real service/source, Codex should execute `ai/tasks/TELEGRAM_AI_CONTROL_PLANE_REPAIR.md` from the MiniPC or the report should be returned to ChatGPT for a targeted patch.
